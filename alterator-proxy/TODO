see also https://bugzilla.altlinux.org/show_bug.cgi?id=14059

- basic proxy setup: [done]
  http_proxy, https_proxy, ftp_proxy (usual naming)
  HTTP_PROXY, HTTPS_PROXY, FTP_PROXY (for Konqueror)
  in /etc/profile.d/proxy.sh

- auth proxy setup (login, password) [done]

- user/root setup (user: ~/.xsession.d/proxy?)

- NTLM auth proxy option?

- *.csh?

- replace "static linking" of additional debug functions 
  which should go to alterator-sh-functions when these are there
